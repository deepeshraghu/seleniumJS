version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      - aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 583420665732.dkr.ecr.ap-south-1.amazonaws.com
  build:
    commands:
      - echo Building the Docker image...
      - docker build -t seleniumjs-nodejs .
      - docker tag seleniumjs-nodejs:latest <your-account-id>.dkr.ecr.<your-region>.amazonaws.com/seleniumjs:latest
  post_build:
    commands:
      - echo Pushing the Docker image to ECR...
      - docker push 583420665732.dkr.ecr.ap-south-1.amazonaws.com/seleniumjs:latest

artifacts:
  files:
    - '**/*'